#!/usr/bin/python3
def element_at(my_list, idx):
    listLen = len(my_list) - 1
    if idx < 0 or idx > listLen:
        return (myaList)
    else:
        my_list[idx] = element
        return (my_list)
